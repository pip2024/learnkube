apiVersion: v1
kind: Service
metadata:
  name: learnkube-headless
  labels:
    app: learnkube-stateful
spec:
  clusterIP: None
  selector:
    app: learnkube-stateful
  ports:
    - port: 8080
      targetPort: 8080
